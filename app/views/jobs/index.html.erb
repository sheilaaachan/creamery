<div class="row">
	<div class="span6">
		<h2>Active Jobs</h2>
		<%= render :partial => "active_jobs" unless @jobs.empty? %>
	</div>
	<div class="span6">
		<h2>Inactive Jobs</h2>
		<%= render :partial => "inactive_jobs" unless @inactive_jobs.empty? %>
		
	</div>
</div>

<%= link_to raw("<i class='icon-plus icon-white'></i> Add a New Job"), new_job_path, :class => 'btn btn-primary' %>
