<h2>Welcome back, <%= current_user.employee.proper_name %></h2>

<p> Admin : I need to add dashboard requirements here:</p>
<div class="row">
	<div class="span6">
		<h3>Active Stores</h3>
			<table class="table table-striped">
				<thead>
			    <tr>
			      <th>Store Name</th>
						<th>Number of current employees</th>
						<th>Number of shift hours in the past two weeks</th>
			      <th>&nbsp;</th>
			    </tr>
			  </thead>
			  <tbody>
			    <% @active_stores.each do |store| %>
			      <tr>
			        <td><%= link_to store.name, store_path(store) %></td>
							<td><%= "## I have no idea ##" %></td>
							<td><%= "## I have no idea ##" %></td>
			        <td>
								<%= link_to raw("<i class='icon-file'></i> Assign a employee"), assignment_path(:new, :store => store), :class => 'btn btn-mini' %>
			        </td>
			      </tr>
			    <% end %>
			  </tbody>
			</table>
	</div>

	<div class="span6">
		<h3>Star Employees</h3>
			<table class="table table-striped">
			  <thead>
			    <tr>
			      <th>Name</th>
						<th>Hours worked</th>
			      <th>Store</th>
			    </tr>
			  </thead>
			  <tbody>
			    <% @active_employees.each do |employee| %>
			      <tr>
			        <td><%= link_to employee.name, employee_path(employee) %></td>
							<td><%= "hours" %></td>
			        <td><%= link_to employee.stores.name, store_path(employee.current_assignment.store) %></td>
			      </tr>
			    <% end %>
			  </tbody>
			</table>
	</div>